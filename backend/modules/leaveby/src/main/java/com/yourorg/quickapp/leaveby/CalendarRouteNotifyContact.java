package com.yourorg.quickapp.leaveby;

/** Stub notify contact for a pickup stop (local UI only until push delivery). */
public record CalendarRouteNotifyContact(CalendarRouteNotifyChannel channel, String to) {}
